export interface ImgData {
  id: number,
  src: string
}

export interface FileWithName {
  raw: File,
  name: string
}

export interface InterfacePhoto {
  likeCount?: number,
  url?: string,
  id?: string
}
